rock the data with 1, 2, 3, 4, 4, 5, 4, 3, 2, 2, 2, 1, 2, 1, 2, 1 , 2, 1

groupify takes the list
  rock the counts
  let the counts at 1 be 888
  let the counts at 2 be 777
  for every number in the list
    if the counts at the number
      shout "yes"
      let the value be the counts at the number
      build the value up
      shout the number with ": " with the value
      let the counts at the number be the value
    otherwise
      shout "no"
      let the counts at the number be 1
    yeah
  baby
  debug the counts
  give back the counts
baby
debug the data

let the result be groupify taking the data
debug the result

